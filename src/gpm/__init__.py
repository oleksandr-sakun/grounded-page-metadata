"""grounded-page-metadata: deterministic batch metadata processor.

Code owns the page manifest, candidate generation, validation and export.
The model advises. It never owns a row, a name, or a chapter.
"""
__version__ = "0.1.0"
